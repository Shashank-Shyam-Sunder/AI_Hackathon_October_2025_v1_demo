# ============================================================
# AI Cost Estimator — Example Environment File
# Copy this file to ".env" and fill in your API keys.
# ============================================================

# Provide valid API keys for one or more LLM providers.
# The app will automatically select the right provider at runtime.

OPENAI_API_KEY=your-openai-api-key
GOOGLE_API_KEY=your-google-api-key
PERPLEXITY_API_KEY=your-perplexity-api-key
GROK_API_KEY=your-grok-api-key

# ------------------------------------------------------------
#  Notes
# ------------------------------------------------------------
# - When using an LLM for task detection, expect some variability
#   in responses even though key parameters (temperature=0, top_p=1)
#   are kept constant. This is especially noticeable with Perplexity.
#   OpenAI generally gives more stable results.
#
# - The Google and Grok integrations have not been fully tested yet,
#   but you are welcome to experiment with them and report results.
